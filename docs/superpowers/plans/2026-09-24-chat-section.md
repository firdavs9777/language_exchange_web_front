# Chat Section Implementation Plan

> **For agentic workers:** execute task-by-task with a fresh implementer per task and a review after each. Steps use `- [ ]`.

**Goal:** make `/chat/:userId` behave like the app: the other person's name and avatar open their member page, the "⋯" button opens a real info panel (profile, media, mute, wallpaper, block/unblock, report, delete), block and report hit the endpoints that exist, message history loads beyond the last 50, the chat surface uses the design system's colours with dark mode and per-conversation wallpapers, and the dead half of the chat folder is removed.

**Input:** `.superpowers/sdd/chat-inventory.md` (read first — routes, live vs dead files, backend routes, socket events, app features, colours). App reference (read-only): `/Users/davis/Desktop/Personal/language_exchange_flutter_application/bananatalk_app/lib/pages/chat/**` (`dialogs/chat_options_menu.dart`, `wallpaper/wallpaper_picker_screen.dart`, `message/message_bubble.dart`).

## Global Constraints

- Frontend `/Users/davis/Desktop/Personal/language_exchange_web_front`, branch `feat/chat` off `main` after Phase D merges (the socket hook `useSocket.ts` changes in D2). **No `Co-Authored-By` trailers. Commit with an explicit pathspec.** No backend work is required; if a backend fix is unavoidable, branch `fix/chat-*` off backend `main` and document the contract.
- Tests `CI=true npx react-scripts test --testPathPattern=<pattern>` (foreground). TypeScript 3.7.2 (no `import type`, no `export type {}`; the build is the type check). `/chat/*` is an authenticated lazy group — not prerendered — but the codebase rule holds: no `window`/`localStorage` reads during render; URL state via react-router.
- Design system only for anything touched: Tailwind + `src/design/*` (Avatar, Badge, ConfirmDialog, DialogShell, notify), `lucide-react`; no react-bootstrap, hex colours, inline styles or emoji icons in changed files; chat colours come from tokens (Task H3) — never new hex.
- Copy via `t("chatPage.<key>") || "English"`; every new key lands in all 18 locales in the final task; `localeParity` NAMESPACES gains `chatPage` and `newChat` if they are in parity (check first).
- Every backend call must match `routes/*.js` in `/Users/davis/Desktop/Personal/language_exchange_backend_application` (the inventory lists them); the report endpoint is `POST /api/v1/reports` with `{ type: "user" | "message", reportId?, reportedUser, reason, description? }`.

---

### Task H1: wiring — profile navigation, info panel, block/report/mute, correct endpoints
**Files:** `src/store/slices/usersSlice.ts` (`reportUser` → `POST /api/v1/reports` with the body above; `getBlockedUsers` → `GET /api/v1/users/:userId/blocked`; add `getBlockStatus(targetUserId)` → `GET /api/v1/users/:userId/block-status/:targetUserId`, `unblockUser` → `DELETE /api/v1/users/:userId/block`; tests in `src/store/slices/usersEndpoints.test.ts` or the existing users test), `src/components/profile/parts/ProfileActions.tsx` (report call adapts to the new body; Unblock when already blocked — test), `src/components/chat/ChatContent.tsx` (header: name + avatar become a `<Link to="/community/:userId">` with an accessible label; "⋯" opens the new panel; presence line stays), new `src/components/chat/ChatInfoPanel.tsx` (+test; a right-side drawer on desktop, bottom sheet on phones, via `DialogShell`): sections **Profile** (avatar, name, languages pill, "View profile" → `/community/:userId`), **Media** (→ `/chat/:userId/media`, fixing that page to use the user id), **Notifications** (Mute/Unmute → `muteConversation`/`unmuteConversation` with the conversation id from `getConversation`), **Wallpaper** (opens Task H3's picker; placeholder entry until H3), **Auto-translate** (per-conversation toggle if `ChatContent` already has one — reuse its state; else omit), **Block/Unblock** (live status via `getBlockStatus`; `ConfirmDialog`; after block navigate to `/chat`), **Report** (reason picker + description → `reportUser`; success toast), **Delete conversation** (`deleteConversation`, confirm, navigate to `/chat`). `UsersList.tsx`: avatar click → `/community/:userId` (row click still opens the chat), kebab gains Mute/Unmute; delete modal body uses a full-sentence key `chatPage.deleteModal.body` with `{{name}}` (replace the fragment). Delete `ChatSettings.tsx`/`.scss` (superseded by the panel) and the `/chat/:conversationId/settings` route; the Search link target does not exist — omit it.
- [ ] TDD; commit `feat(chat): profile links, info panel with mute, block, report and delete; report and block endpoints fixed`.

### Task H2: message history, bubbles, live indicators
**Files:** `src/store/slices/chatSlice.ts` (`getConversation` takes `{ senderId, receiverId, page, limit }`; `serializeQueryArgs` by conversation, `merge` older pages in front, `forceRefetch` on page change — the RTK "infinite" pattern), `ChatContent.tsx` (scroll-to-top sentinel loads the previous page and preserves the scroll offset; "Loading earlier messages" row; extract `MessageBubble.tsx` (memoized) from the inline `.map` with the same markup/test ids, date separators, tick status from the existing socket events (`messageDelivered`/`messagesRead`) and typing indicator from `userTyping` if `ChatContent` does not already show them — verify first; link previews via the existing `utils/linkDetector.ts` rendered as a small card (title/host only, no fetch); `loading="lazy"` on every `<img>`), tests: `ChatContent.pagination.test.tsx` (page 2 requested on sentinel, order preserved, offset kept), `MessageBubble.test.tsx`, `linkDetector.test.ts`.
- [ ] TDD; commit `feat(chat): load older history on scroll, memoized bubbles, receipts and typing from the socket, link cards`.

### Task H3: colours, dark mode, wallpapers
**Files:** `src/index.css` (chat tokens: `--chat-bg`, `--chat-bubble-out`, `--chat-bubble-out-text`, `--chat-bubble-in`, `--chat-bubble-in-text`, `--chat-line`, `--chat-muted` — light + dark values from the existing brand/ink scale; the outgoing bubble uses `brand-deep`→`brand` gradient, incoming uses `ink-50`/`ink-900` dark), `ChatContent.css`, `MainChat.css`, `UsersList.css` (replace every hex with the tokens; add dark-mode rules through the tokens so one `@media (prefers-color-scheme: dark)` block per file suffices), `src/store/slices/chatSlice.ts` (`getConversationTheme(id)` → `GET /conversations/:id/theme`, `setConversationTheme({ id, theme })` → `PUT`; tag `ChatTheme`), new `src/components/chat/WallpaperPicker.tsx` (+test): the free presets the app ships (read `wallpaper_picker_screen.dart` for names/gradients; skip the coin-gated premium pack — note it) plus "Default"; applies `--chat-bg` on the conversation pane; saved via `setConversationTheme`; `useSocket` listener for `themeChanged` updates the cache; `ChatInfoPanel` Wallpaper entry opens it. `src/components/chat/chatStyling.test.ts`: no hex, no inline styles in the chat files; every colour reference is a token.
- [ ] TDD; commit `feat(chat): design tokens with dark mode, shared conversation wallpapers`.

### Task H4: dead code and i18n hygiene
**Files:** delete the dead parallel tree listed in the inventory (`components/{ChatHeader,ConversationItem,MessageBubble(old),MessageInput,OnlineStatus,ReadReceipt,TypingIndicator,EmojiPicker,ReactionPicker,VoiceRecorder,VoicePlayer,MediaPreview,ReplyPreview}` + SCSS + `components/index.ts`, `hooks/{useTyping,useVoiceRecorder,index}`, `utils/{emojiUtils,messageFormatter,index}` (keep `linkDetector.ts`, now live), `MessageList.*`, `ConversationList.*`, `ChatInput.*`, `actions/BookmarksView.tsx` unless a route uses it, `type.ts` if unused) after `grep` proves zero importers; `MediaGallery.tsx` uses the user id param and is reachable from the panel; `useSocket.ts` debug listeners removed (keep error logging behind `process.env.NODE_ENV !== "production"`); every `t(key) || "English"` in `ChatContent.tsx`, `UsersList.tsx`, `NewChat.tsx`, panel and picker has its key in `eng.json` (script it); fragment-concatenated strings replaced by full sentences with placeholders; `communityStyling`-style guard extended to chat.
- [ ] Commit `chore(chat): remove the dead parallel chat implementation; every string keyed`.

### Task H5: locales, verification, merge
- [ ] Inventory new/changed `chatPage.*` keys → 18 locales via merge script; parity namespaces.
- [ ] Full suite; build; prerender (10 routes); `BUNDLE_BUDGET=1 npm run test:budget` (chat is lazy — main must not grow); Playwright with a mocked session at 390/1280: `/chat/<id>` header name → `/community/<id>`, "⋯" panel opens with all entries, Block confirm calls `POST /users/:id/block`, Report calls `POST /reports` with `type: "user"`, scroll-up loads page 2, wallpaper change persists via `PUT /conversations/:id/theme`, dark mode (`colorScheme: "dark"` context) renders token colours, no console errors, no overflow, no raw keys; screenshots.
- [ ] Merge `feat/chat` `--no-ff`; push.

## Not in this plan
Calls (mobile-only by design), disappearing messages, polls in chat, secret chats, nicknames, quick replies, coin-gated premium wallpapers, message search page.
