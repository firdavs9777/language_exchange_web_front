# Community Section Implementation Plan

> **For agentic workers:** execute task-by-task with a fresh implementer per task and a review after each. Steps use `- [ ]`.

**Goal:** bring the community section up to the mobile app: the member detail page at `/community/:id` becomes the redesigned profile page plus the app's match-aware blocks (language match card, mutual interests, conversation starters, suggested members, Moments/About tabs on phones); the member list gets URL-shareable filters, sort and tabs (For you / All / Online / New / Nearby / Topics), a "For you" tab on the backend's recommendation engine, translated quick chips, and a lighter, faster list.

**Input:** `.superpowers/sdd/community-inventory.md` (routes, files, endpoints, backend params, Flutter reference — read it first). App reference: `/Users/davis/Desktop/Personal/language_exchange_flutter_application/bananatalk_app/lib/pages/community/single/single_community_screen.dart` and `tabs/partner_discovery_tab.dart` (read-only; the web mirrors their semantics, not their widgets).

## Global Constraints

- Frontend `/Users/davis/Desktop/Personal/language_exchange_web_front`, branch `feat/community` off `main` (1f95269); backend `/Users/davis/Desktop/Personal/language_exchange_backend_application`, branch `feat/community` off `main` (99f8ae1). **No `Co-Authored-By` trailers. Commit with an explicit pathspec.**
- Frontend tests `CI=true npx react-scripts test --testPathPattern=<pattern>` (foreground). Baseline 149 suites + 1 skipped / 1376 tests. TypeScript 3.7.2 (no `import type`, no `export type {}`; the build is the type check). Backend: `nvm use 24`, `node --test test/<file>.test.js`; only `authSecretRotation` may fail.
- `/communities` is prerendered (logged-out → `PublicCommunities`): no browser globals during render; `renderRoute.test.tsx` stays green. `/community/:id` and the signed-in list are lazy, not prerendered — still no globals during render (codebase rule) and URL state via react-router (`useSearchParams`), never `window.location`.
- Design system only: Tailwind + `src/design/*` + `src/components/profile/parts/*`; lucide icons; no react-bootstrap, hex colours, inline styles or emoji icons; delete SCSS/CSS that only served replaced markup. `communityStyling.test.ts` extends to every file this plan touches.
- Copy via `t("communityMain.<key>") || "English"` / `communityDetail.*` / `profile.*` (reuse existing keys — the inventory lists the namespaces); new keys land in all 18 locales in the final task; `localeParity` NAMESPACES gains `communityMain`, `communityDetail` if they are in parity (check first).
- Backend contracts (Task C0) are consumed verbatim; every list request must remain answerable by `GET /api/v1/auth/users` + `GET /api/v1/matching/recommendations`.

---

### Task C0 (backend): indexes, recommendations contract, engagement fields
**Files:** `models/User.js` (add indexes: `{ gender: 1 }`, `{ "location.country": 1 }`, compound `{ native_language: 1, language_to_learn: 1, isOnline: -1, lastActive: -1 }` if not present; store a lowercase `location.countryKey` ONLY if a case-insensitive regex on country is currently unindexable — otherwise leave the query alone and document), `controllers/users.js` (`USER_PUBLIC_FIELDS` += engagement fields that already exist on the model and are safe: e.g. `responseRate`, `avgResponseMinutes`, `createdAt` (for "new"), `lastSeenAt` subject to privacy — read the model; add nothing that does not exist), `controllers/matching.js` (`getRecommendations`: confirm it is behind `protect`, its response shape `{ success, data: [...] }` with the same user projection the list uses, `limit` param, and that it excludes blocked users; add `page`/`limit` if missing, capped at 50), `test/communityIndexes.test.js` (indexes exist via `User.schema.indexes()`), `test/matchingRecommendations.test.js` (shape, limit cap, exclusions), extend an existing users test for the new public fields.
- [ ] TDD; `npm test | tail -14`; commit `feat(community): list indexes, recommendation contract, engagement fields on public profiles`.

### Task C1: `/community/:id` is the profile page with match-aware blocks
**Files:** create `src/components/profile/parts/LanguageMatchCard.tsx` (+test): match type from viewer vs target `native_language`/`language_to_learn`: `perfect` (each natively speaks what the other learns), `youLearnTheirs`, `theyLearnYours`, `sameNative`, `none`; copy per type (app wording, see the Flutter widget) with a `LanguageExchangePill`; hidden when logged out or the viewer has no languages; `MutualInterests.tsx` (+test): intersection of `topics`, chips, hidden when empty; `ConversationStarters.tsx` (+test): 3 starters chosen deterministically from a keyed template list (by match type, shared topics, target's learning language) — all copy via i18n keys with `{{name}}`/`{{language}}` placeholders; each has a "Send" button that opens the chat with the starter prefilled (`createChatRoom` then navigate to `/chat/:id?draft=<encoded>`; read how the chat page reads drafts — if it does not, add `draft` support minimal in the chat input) — hidden when logged out; `SuggestedMembers.tsx` (+test): up to 8 `MemberCard`s (reuse `src/components/community/MemberCard.tsx`) from `useGetCommunityMembersQuery` filtered like today's "Suggested for you", excluding the target and the viewer; `EngagementStats.tsx` (+test): response rate / "New member" (createdAt < 14 days) when the public fields exist, hidden otherwise. Modify `src/components/profile/ProfilePage.tsx` (+test): for other users render LanguageMatchCard + EngagementStats under the header, MutualInterests inside the left column, ConversationStarters above Moments, SuggestedMembers at the bottom; below 1024px a two-tab switcher **Moments / About** (app parity: About = languages, about, learning, photos, mutual interests; Moments = starters + moments grid), URL-synced with `?tab=`; desktop keeps two columns. `src/router/routes.tsx`: `community/:id` renders `ProfilePage` (same lazy group); delete `CommunityDetail.tsx`, `CommunityDetail.css`, `TandemMemberCard` if unused, dead props; update `routes.test.tsx`, `communityStyling.test.ts`.
- [ ] TDD; commit `feat(community): member page = profile page + language match, mutual interests, starters, suggestions, Moments/About tabs`.

### Task C2: list state in the URL
**Files:** `src/components/community/MainCommunity.tsx`, `lib/filterStorage.ts`, new `lib/communityUrlState.ts` (+test): encode/decode filters, `sort`, `search`, `tab` to/from `URLSearchParams` (short stable keys: `native`, `learning`, `age`, `gender`, `country`, `level`, `topics`, `mutual`, `online`, `new`, `sort`, `q`, `tab`); precedence on mount: URL → localStorage default → empty; every change writes the URL with `setSearchParams(…, { replace: true })` and mirrors to localStorage; Back restores; `ActiveFilterChips` remove keys from the URL; "Copy link" button in the filter sheet footer (clipboard in a handler). Tests: round-trip, precedence, back navigation via `createMemoryRouter`.
- [ ] TDD; commit `feat(community): shareable filters, sort and tab in the URL`.

### Task C3: tabs and "For you"
**Files:** `CommunitySubNav.tsx` (+test), `MainCommunity.tsx`, `communitySlice.ts` (`getRecommendations({ limit })` → `GET /api/v1/matching/recommendations`), `QuickFilterChips.tsx` (+test; i18n all four chips), `CommunityFilterSheet.tsx` (+test: every control changes the right filter; "Clear all"; live count), `ActiveFilterChips.test.tsx`. Tabs: **For you** (recommendations; empty state explains what to fill in, links to `/profile/edit`), **All** (filters as today), **Online** (`onlineOnly=true` preset), **New** (`newUsersOnly=true`), **Nearby** (existing `/community/nearby`), **Topics** (existing `/topics`); tab in the URL (`?tab=`); the visitors banner and highlighted carousel stay on All only. Keep the wave sheet.
- [ ] TDD; commit `feat(community): For you, Online and New tabs; translated quick chips; tested filter sheet`.

### Task C4: a faster list
**Files:** `MemberCard.tsx` (`loading="lazy"`, `decoding="async"`, fixed aspect boxes, `React.memo` with a shallow row comparator), `MainCommunity.tsx` (IntersectionObserver sentinel replaces the "Load more" button — keep the button as the no-IO fallback; `content-visibility: auto` + `contain-intrinsic-size` on card wrappers via a Tailwind arbitrary class; skeleton rows sized like cards; count query only while the sheet is open, debounced ≥ 400 ms; abort superseded page requests via RTK's `skip`/arg change), `src/components/community/communityPerf.test.tsx` (mount → exactly 3 requests; scrolling the sentinel into view requests page 2 once; changing a filter resets to page 1 and does not append).
- [ ] Measure before/after with Playwright (`performance.getEntriesByType("resource")` count and total bytes on `/communities` signed-in with 40 mocked members); commit `perf(community): lazy media, memoized cards, sentinel paging, content-visibility`.

### Task C5: locales, verification, merge
- [ ] Inventory new keys (`communityDetail.match.*`, `communityDetail.starters.*`, `communityMain.tabs.*`, `communityMain.chips.*`, …) → 18 locales via merge script; parity namespaces.
- [ ] Full suite; build; prerender (10 routes); `BUNDLE_BUDGET=1 npm run test:budget`; Playwright with a mocked session at 390/1280: `/communities?tab=all&native=ko&online=1` (chips reflect the URL; back restores), `?tab=foryou`, `/community/<id>` (match card, starters, tabs on phone), logged-out `/community/<id>` (public endpoint, no starters), `/communities` logged-out unchanged; no console errors, no overflow, no raw keys; screenshots.
- [ ] Merge both `feat/community` branches `--no-ff`; push.

## Not in this plan
Swipe "Quick Match" mode, VIP upsell banner, story highlights on the member page, waves redesign, Bootstrap retirement outside touched files.
