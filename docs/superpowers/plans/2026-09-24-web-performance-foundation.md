# Web Performance and Foundation (Phase D) Implementation Plan

> **For agentic workers:** execute task-by-task with a fresh implementer per task and a review after each. Steps use `- [ ]`.

**Goal:** make the marketing pages fast enough for Core Web Vitals to help rather than hurt search ranking, and clear the two foundations Phase B left open: locale JSON loaded on demand (it is ~306 KB of the 552 KB gzipped main chunk), `socket.io-client` only for signed-in users, self-hosted fonts with per-script tracking resets, and a consent-withdrawal UI so the GA measurement id can finally be set.

**Spec:** `docs/superpowers/specs/2026-09-22-reach-marketing-admin-design.md` §4.6 (truthful measurement, consent), §5.6 (weight). Follow-ups recorded in the Phase B ledger and `src/seo/bundleBudget.test.ts`'s comment.

## Global Constraints

- Repo `/Users/davis/Desktop/Personal/language_exchange_web_front`, branch `feat/perf-foundation` off `main` (e8ec2c0). **No `Co-Authored-By` trailers. Commit with an explicit pathspec (`git commit -m … -- <paths>`), never bare `git commit`.**
- Tests `CI=true npx react-scripts test --testPathPattern=<pattern>` (foreground). Baseline 126 suites / 1003 tests (+1 skipped suite `bundleBudget`, run with `BUNDLE_BUDGET=1 npm run test:budget` after a build). TypeScript 3.7.2: no `import type`, no `export type {}`; the build is the type check.
- Prerender discipline: `renderRoute.tsx` renders every `SEO_PAGES` path in English in Node via `scripts/register.js` (Babel); no browser globals during render; the English-first hydration contract in `src/utils/hydrationLanguage.ts` stays: first client render is English, the visitor's language is restored in an effect (`App.tsx`).
- Copy idiom `t("key") || "English"`; new keys land in all 18 locales via `scripts/i18n/merge-keys.js` in the same task that adds them; `localeParity.test.ts` NAMESPACES covers every touched namespace.
- Every task ends green on: its pattern, the full suite, `GENERATE_SOURCEMAP=false npx react-scripts build`, `node scripts/prerender.js` (all 10 routes 200), and — where weight changes — `BUNDLE_BUDGET=1 npm run test:budget`.

---

### Task D1: locale JSON on demand
**Files:** `src/utils/i18n.ts` (keep `en` inlined; the 17 others become a `LOCALE_LOADERS: Record<string, () => Promise<{ default: object } | object>>` map of `import("./locales/<file>.json")` calls; replace the unused `i18next-http-backend` with a ~20-line custom backend plugin `src/utils/i18nLazyBackend.ts` — `{ type: "backend", init() {}, read(lng, ns, cb) { loader(lng)().then(m => cb(null, (m as any).default || m), err => cb(err, false)) } }` — with `partialBundledLanguages: true` and `resources: { en: { translation: en } }`; map i18next codes to files as today: `ko→kor.json`, `zh→zho.json`, `zh_TW→zh_TW.json`), `src/utils/hydrationLanguage.ts` (restore is already an effect; `restoreAfterHydration` must `await i18n.changeLanguage(pending)` — i18next loads the bundle before switching, so no flash of keys; verify `parseMissingKeyHandler` still returns "" for the split second before load), `src/seo/prerender/renderRoute.tsx` (English only — unchanged, but confirm the Babel register path can `require` a JSON via the dynamic import when a prerendered page ever calls `changeLanguage`; it must not), `src/seo/bundleBudget.test.ts` (total budget drops: only `eng.json` is inlined now — measure `localesGz` as `eng.json` only, set `BUDGET_KB` = measured + 5% and update the comment; keep `APP_BUDGET_KB`), `src/utils/i18n.test.ts` (new: `changeLanguage("ko")` resolves and `t("home.hero.title")` is Korean afterwards; an unknown language falls back to English; the loader map covers exactly `SUPPORTED_LANGUAGES` minus `en`), plus every existing test that calls `i18n.changeLanguage(<non-en>)` synchronously — grep and `await` them.
**Behaviour:** the language switcher (`LanguageSettings.tsx`, navbar flag menu) shows the current language until the new bundle arrives (i18next keeps the old one; no key flash), and `<html lang>` updates once the change resolves (existing `updateHtmlLang`).
- [ ] TDD; measure before/after; commit `perf(i18n): load the 17 non-English locales on demand`.

### Task D2: socket.io only for signed-in users
**Files:** `src/components/chat/hooks/useSocket.ts` (`SocketProvider` reads `state.auth.userInfo`; when null it renders children with a `null` socket and never imports the client; when signed in, `import("socket.io-client")` inside an effect, connect, and clean up on sign-out — the context value shape stays what chat consumers expect; read every consumer: `grep -rn "useSocket(" src`), `src/App.tsx` (unchanged wrapping; the provider decides), `src/seo/eagerGraph.test.ts` (extend: `socket.io-client` must not be statically reachable from the eager roots), `useSocket.test.tsx` (new: logged out → no import, no connection; logged in → connects with the token; sign-out → disconnects).
- [ ] TDD; commit `perf(chat): connect the socket only for signed-in users, load the client on demand`.

### Task D3: self-hosted fonts and script-aware tracking
**Files:** `package.json` (`@fontsource/inter`, `@fontsource/plus-jakarta-sans` — CSS only, no types needed), `src/index.css` or a new `src/fonts.css` imported from `src/index.tsx` (import only the weights used: Inter 400/500/600/700/800, Plus Jakarta Sans 700/800 — check `grep -rn "font-\(medium\|semibold\|bold\|extrabold\|light\)" src | wc -l` and drop weights with zero use; `font-display: swap` is the package default), `public/index.html` (remove both Google Fonts `<link rel="stylesheet">`s and both `preconnect`s; add `<link rel="preload" as="font" type="font/woff2" crossorigin>` for the two latin woff2 files the homepage first paints with — the hashed paths are only known post-build, so preload via `%PUBLIC_URL%`-independent approach: copy the two woff2 files into `public/fonts/` in a `scripts/fonts.js` postinstall-style step referenced from `package.json` `"prefonts"`… **simpler and preferred:** import the CSS from `src/`, skip preload, accept one round trip), `src/index.css` (`:lang()` resets — `html:lang(ko), html:lang(ja), html:lang(zh), html:lang(zh-TW), html:lang(th), html:lang(ar), html:lang(hi) { --bt-track-tight: 0; --bt-track-display: 0; }` and use those variables where the type system sets negative `letter-spacing` at lines ~66/79/91; CJK/Thai/Arabic/Devanagari must never get negative tracking), `src/seo/eagerGraph.test.ts` or a new `src/fonts.test.ts` (asserts `public/index.html` contains no `fonts.googleapis.com` and the fonts CSS is imported once).
- [ ] Build, measure main.css delta (font-face rules only; woff2 files are separate assets), confirm the homepage screenshot still renders Inter/Plus Jakarta Sans (Playwright: `document.fonts.check('700 16px "Plus Jakarta Sans"')` after `document.fonts.ready`); commit `perf(fonts): self-host Inter and Plus Jakarta Sans; no negative tracking for CJK, Thai, Arabic, Devanagari`.

### Task D4: consent withdrawal
**Files:** `src/analytics/consent.ts` (add `clearConsent()` and an `onConsentChange` subscription; `withdrawConsent()` writes `"denied"`, sets `window["ga-disable-" + GA_ID] = true`, and expires the `_ga*` cookies for the site domain), `src/analytics/ga.ts` (respect `ga-disable` on init; `gaEvent` no-ops when consent is not `"granted"` — read what it does today first), `src/components/growth/ConsentBar.tsx` (add a `mode="manage"` that can be opened programmatically via a tiny store/event — `openConsentManager()` — showing current choice with Accept / Decline buttons; the bar still hides when `REACT_APP_GA_MEASUREMENT_ID` is empty), `src/components/footer/FooterMain.tsx` (link "Privacy choices" → `openConsentManager()`; hidden when the GA id is empty, so today's footer is unchanged), `src/components/settings/PrivacySettings.tsx` (a "Analytics cookies" row with the same control, for signed-in users), `src/components/legal/PrivacyPolicy*.tsx` (grep the privacy policy page: add one sentence linking to the choices control — read the existing copy and keys), locale keys `consent.manage.*` in all 18 files (translate the ~6 short strings yourself; parity namespace `consent` already covered).
- [ ] TDD (`ConsentBar.test.tsx`: manage mode shows current state, decline writes denied + sets the ga-disable flag + clears cookies, re-accept re-enables; footer link present only with a GA id); commit `feat(consent): withdraw analytics consent from the footer, privacy settings and the policy page`.

### Task D5: Turkish brand suffix
**Files:** `src/utils/locales/tr.json` only — normalize every `BananaTalk'ı`, `BananaTalk'ın`, `BananaTalk'a`, `BananaTalk'ta` etc. to the back-rounded forms the Phase B strings use (`BananaTalk'u`, `BananaTalk'un`, `BananaTalk'a` stays, `BananaTalk'ta` stays — apostrophe + suffix decided by the pronunciation /tɔk/: u/un/a/ta/tan/la). List every changed string in the commit body. `localeParity` must stay green (values only).
- [ ] Script the replacement, eyeball the diff, commit `i18n(tr): one pronunciation for the BananaTalk suffixes`.

### Task D6: verification and merge
- [ ] Full suite; build; prerender (10 routes 200, `/404` 404); `BUNDLE_BUDGET=1 npm run test:budget` with the lowered budget; Playwright smoke on `/`, `/download`, `/meet`, `/learn-korean`, `/communities`, `/moments` at 390 and 1280: no console errors, no hydration warnings, fonts loaded, `store_tap` fires, language switch to Korean loads the chunk and re-renders the hero in Korean with no raw keys visible; a logged-out page makes no socket connection (no `socket.io` request in the network log).
- [ ] Merge `feat/perf-foundation` into `main` with `--no-ff`; push; update the Phase B ledger's follow-up list.

## Not in this plan
Bootstrap retirement (its own phase: `!important` utilities collide with Tailwind app-wide), Moments Phase 2 (reels, story viewers, story studio, AI Study), fragment-concatenated i18n strings, deploy automation.
